rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 開発環境用：すべてのドキュメントへのアクセスを許可
    // 本番環境では適切なセキュリティルールに変更してください
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
